# projetos-em-python-python-projects
Vários projetos em python / python projects
 1 - 
 (PT-BR) Algoritmo que lê: número da conta do cliente, saldo, débito e crédito.
Após isso, calcula e escreve o saldo atual (saldo atual = saldo - débito + crédito).
Também testa se saldo atual for maior ou igual a zero escrevendo a mensagem
'Saldo Positivo’, senão escrever a mensagem 'Saldo Negativo’.

(EN) Algorithm that reads: customer account number, balance, debit, and credit.
After that, it calculates and writes the current balance (current balance = balance - debit + credit).
It also tests if the current balance is greater than or equal to zero by writing the message 'Positive Balance', otherwise it writes the message 'Negative Balance'.

2- 
(PT-BR)A partir do valor das duas notas de um aluno, calculei a média e montei o
conjunto de if’s e else’s que verifica se ele foi aprovado, reprovado ou precisará
fazer a final. Considerei >=70 aprovado e <40 reprovado.

(EN)Based on the values ​​of a student's two grades, I calculated the average and created a set of if and else statements that check if the student passed, failed, or needs to take the final exam. I considered >=70 to pass and <40 to fail

3-
(PT-BR)Após informar a situação do aluno no projeto 2, mostrei o conceito
que ele ficou de acordo com os dados a seguir: entre 90 e 100 = Conceito A / entre 70 e 90 = Conceito B / entre 40 e 70 = Conceito C / entre 00 e 40 = Conceito D

(EN)After informing the student about their situation in project 2, I showed the grade they received according to the following data: between 90 and 100 = Grade A / between 70 and 90 = Grade B / between 40 and 70 = Grade C / between 00 and 40 = Grade D

4-
(PT-BR)Escrevi um programa que lê o número de maçãs compradas, calcula e escreve o
custo total da compra. (maçãs custam R$ 1.30 cada, se forem compradas menos de
uma dúzia, e R$ 1.00 se forem compradas pelo menos 12 unidades).

(EN)I wrote a program that reads the number of apples purchased, calculates and writes the total cost of the purchase. (Apples cost R$ 1.30 each if less than a dozen are purchased, and R$ 1.00 if at least 12 units are purchased).
